numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
pandas>=2.0
pytest>=7.0
# DERN Stage B (real-model cascade on Apple Silicon). Optional: only needed to
# run the src/dern_b/ harness + its heavy tests. Apple-Silicon (arm64) only.
mlx-lm>=0.31 ; platform_machine == "arm64"
