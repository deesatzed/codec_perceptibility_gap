# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Build / dist
dist/
build/

# Archives kept out of history (large/binary snapshots)
codec_codex_experiments_v0_2.zip

# LaTeX / tectonic build intermediates (the .pdf/.tex/.docx outputs ARE tracked)
*.aux
*.log
*.out
*.toc

# Generated documents are tracked (regenerable from source markdown via pandoc + tectonic).
# To exclude them instead, uncomment:
# *.docx
# *.tex
# *.pdf

# CRC answer cache (real generations, regenerable)
codec_codex_experiments/results/crc_cache.json
codec_codex_experiments/results/crc_twoarm_cache.json
codec_codex_experiments/results/crc_twoarm3_cache.json
codec_codex_experiments/results/crc_eval_*.json
codec_codex_experiments/results/agent_skill_audit.json

# carbon_audit fetched public data (regenerable from public sources, large)
codec_codex_experiments/carbon_audit/data/
